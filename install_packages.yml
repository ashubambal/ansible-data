---
- name: This playbook install packages
  hosts: webserver
  become: true
  tasks:
  - name: install package
    yum: 
      name: ['git', 'make', 'gcc', 'wget', 'telnet', 'gzip']
      state: installed 
